-	posts.foreach(function(post, index) {
-	if (index % 3 == 0) {
		div.row
-	}
		div.span4
			h2 a(href='/u/' + post.user)= post.user
				| say
			p 
				small= post.time
			p= post.post

-})
-	if(posts.length % 3 != 0) 
	div.row